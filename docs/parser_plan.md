# Parser Plan

## フェーズ 1: CSV / Excel エクスポート (現在)
公式TXTを何らかの手段でCSV化したものを、`column_aliases.json` で吸収して正規化。

## フェーズ 2: 固定長テキスト直パース
`KSE*.TXT` 等の固定長・改行ありの特殊フォーマットを Python で直接読み込むパーサーの実装。

1. 行先頭の識別記号による分岐
2. スライス指定による列抽出
3. 場コード、級別コードのマスター変換
